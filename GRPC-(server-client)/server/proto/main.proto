syntax = "proto3";

package calculator;

option go_package = "/proto/gen;genpb";

service Calculator {
    rpc Add (AddRequest) returns (AddResponse);
}

message AddRequest {
    int32 A = 1;
    int32 B = 2;
}

message AddResponse {
    int32 Sum = 1;
}